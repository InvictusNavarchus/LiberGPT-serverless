{
  "name": "liber-gpt-serverless",
  "version": "0.1.0",
  "description": "A discord bot that supports slash commands deployed as a serverless function.",
  "private": true,
  "scripts": {
    "sync": "slash-up sync",
    "sync:dev": "slash-up sync -e development",
    "build": "tsc"
  },
  "dependencies": {
    "slash-create": "^5.6.1"
  },
  "devDependencies": {
    "@types/node": "^16.7.13",
    "@vercel/node": "^1.12.1",
    "slash-up": "^1.1.2",
    "typescript": "^4.4.2"
  }
}
